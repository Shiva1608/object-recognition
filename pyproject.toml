[project]
name = "iot-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "ultralytics",
    "pillow",
    "numpy",
    "python-multipart",
    "pyttsx3>=2.99",
    "python-dotenv>=1.1.1",
]
